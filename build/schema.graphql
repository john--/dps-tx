type Transfer @entity {
  id: ID!,
  from: Bytes!
  to: Bytes!
  value: BigInt!,
  blockNumber: BigInt!,
  timestamp: BigInt!
}

type Count @entity {
  id: ID!,
  transfersOut: Int!
  transfersIn: Int!,
  lastTx: BigInt!
}

