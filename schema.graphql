type Transfer @entity {
  id: ID!,
  from: Bytes!
  to: Bytes!
  value: BigInt!,
  blockNumber: BigInt!,
  timestamp: BigInt!
}

type Count @entity {
  id: ID!,
  numTransfersOut: Int!
  numTransfersIn: Int!,
  totalIn: BigInt!,
  totalOut: BigInt!,
  lastTx: BigInt!
}

